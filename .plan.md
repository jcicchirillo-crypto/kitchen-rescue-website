# Add Trade Quick Quote Builder (Matching Trade Partners Page Style)

## Overview
Add a simplified quote builder for builders/trade partners that links from the trade-partners page. The page will match the exact styling of `trade-partners.html` (dark theme, same layout, fonts, colors).

## Current State
- Main site is static HTML (not React)
- Trade partners page has dark theme with specific styling
- Existing quote calculation logic exists in `availability.html` (lines 674-795)
- API server has `/send-quote-email` endpoint
- Trade partners page is at `public/trade-partners.html`

## Implementation Plan

### 1. Create API Endpoints (if needed)
Check if `/api/quote/calculate` and `/api/quote/send` exist, or create them in `api/server.js`:
- `/api/quote/calculate` - Calculate quote based on postcode, weeks, builderUplift, includeReferral
- `/api/quote/send` - Send quote email to builder with referral code if applicable

### 2. Create Trade Quote Page (`public/trade-quote.html`)
**Match trade-partners.html styling exactly:**
- Same dark gradient background: `radial-gradient(circle at 20% 0%, #1a1a1a 0%, #040404 50%, #010101 100%)`
- Same CSS variables and color scheme
- Montserrat font family
- Same header/navigation structure
- White content boxes on dark background (matching trade page style)
- Red accent color (#dc2626 or #e63946)
- Same button styles (`.btn-small`, `.btn-small.accent`)
- Same max-width: 1080px, padding: 48px 36px
- Convert React component to vanilla JavaScript/HTML

**Form Structure:**
- White content box with form fields: postcode, weeks, start date, builder uplift, referral checkbox
- Calculate button (red accent style)
- Quote summary display (white box)
- Email quote section (white box)

### 3. Reuse Existing Quote Logic
- Extract/adapt the pricing calculation from `availability.html`
- Use the distance calculation logic (distanceMap)
- Calculate base hire, delivery/collection costs
- Add builder uplift calculation
- Handle referral code generation

### 4. Add Link from Trade Partners Page
Add a prominent button/link in `trade-partners.html` that links to `trade-quote.html`
- Use the same button styling (`.btn-small.accent`)
- Place it in a logical location (maybe in the hero section or referral section)

### 5. Styling Details (Match Trade Partners)
- Background: Dark gradient matching trade-partners.html
- Content boxes: White (#ffffff) with rounded corners, shadows
- Text: Dark text on white boxes, white text on dark background
- Buttons: Red accent buttons matching `.btn-small.accent` style
- Form inputs: Styled to match trade page aesthetic
- Typography: Montserrat font, same sizes and weights

## Files to Create/Modify

1. **Create**: `public/trade-quote.html` - New quote builder page (matching trade-partners.html style)
2. **Modify**: `api/server.js` - Add quote calculation and send endpoints (if needed)
3. **Modify**: `public/trade-partners.html` - Add link/button to quote builder

## Technical Details

### Quote Calculation Logic (from availability.html):
- Base hire: £70/day or weekly rate
- Delivery/collection: Based on distance bands (0-50, 51-100, 101-150, 151-200, 201-300 miles)
- Distance calculation: Uses postcode area mapping
- Builder uplift: Additional margin added on top
- Referral code: Generate if `includeReferral` is true

### API Endpoints Needed:
```javascript
POST /api/quote/calculate
Body: { postcode, weeks, builderUplift, includeReferral, startDate }
Returns: { basePrice, deliveryPrice, totalBeforeUplift, totalAfterUplift, distanceMiles }

POST /api/quote/send  
Body: { builderName, builderEmail, postcode, weeks, startDate, builderUplift, includeReferral, quote }
Returns: { success: true, referralCode?: "ABC123" }
```

### Styling Reference:
- Copy CSS variables and base styles from trade-partners.html
- Use same white content box pattern
- Match button styles exactly
- Same spacing and layout principles
